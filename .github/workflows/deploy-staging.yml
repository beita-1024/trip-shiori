
# ステージング環境用デプロイ
# デバッグのしやすさを考えてCapRoverを選択
name: Staging Deploy

# ワークフロートリガー設定
# - push: mainブランチまたはinfra/add-cd-caproverブランチにプッシュ時
# - workflow_dispatch: 手動実行時（ブランチ選択可能）
on:
  push:
    branches:
      - main
      - infra/add-cd-caprover
  workflow_dispatch:
    inputs:
      branch:
        description: "デプロイ対象ブランチ"
        required: true
        default: "main"
        type: string

jobs:
  deploy-caprover:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch || github.ref }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Deploy to CapRover
        env:
          CAPROVER_URL: ${{ secrets.CAPROVER_URL }}
          CAPROVER_TOKEN_FE: ${{ secrets.CAPROVER_TOKEN_FE }}
          CAPROVER_TOKEN_BE: ${{ secrets.CAPROVER_TOKEN_BE }}
          CAPROVER_APP_FE: ${{ secrets.CAPROVER_APP_FE }}
          CAPROVER_APP_BE: ${{ secrets.CAPROVER_APP_BE }}
        run: |
          echo "🚀 Deploying to CapRover..."
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"
          echo "Trigger: ${{ github.event_name }}"
          
          # 環境変数の確認
          if [ -z "$CAPROVER_URL" ]; then
            echo "❌ CAPROVER_URL is not set"
            exit 1
          fi
          if [ -z "$CAPROVER_TOKEN_FE" ] || [ -z "$CAPROVER_TOKEN_BE" ]; then
            echo "❌ CapRover tokens are not set"
            exit 1
          fi
          if [ -z "$CAPROVER_APP_FE" ] || [ -z "$CAPROVER_APP_BE" ]; then
            echo "❌ CapRover app names are not set"
            exit 1
          fi
          
          # Makefileを使用してCapRoverにデプロイ
          make deploy-cap
          
          echo "✅ CapRover deployment completed successfully"
